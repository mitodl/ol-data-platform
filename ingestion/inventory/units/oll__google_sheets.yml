---
# Hand-written: this unit has no Airbyte connection, so bin/airbyte-inventory.py
# cannot produce it. See docs/specs/INGESTION_INVENTORY_SPEC.md §3.
schema_version: 1
deployment: oll
layer: google_sheets
scope: scoped
strategies:
  qa: omit
  local: fixture
loader: dlt
table_prefix: raw__oll__google_sheets__
dlt:
  source_module: ol_dlt.sources.oll
  write_disposition: replace
tables:
- name: raw__oll__google_sheets__courses
  raw_table: raw__oll__google_sheets__courses
  sync_mode: full_refresh_overwrite
  primary_key:
  - - readable_id
  modeled: true
