.idea/
**/.mypy_cache/
**/__pycache__/
/.pants.d/
/.vscode/
/.pids/
/dist/
.venv/
bin/pants
**/*.db
**/*.duckdb
**/dagster_local/*
.aider*
.env
**/*.egg-info/
progress.log
**/logs/
# dlt specific
**/.dlt/secrets.toml
**/.dlt/data/
**/.dlt/.sources
**/.dlt/.pipelines
**/.dlt/state/
src/ol_dbt/.user.yml
# Superset
.sup/

# Airbyte inventory working files — regenerate with bin/airbyte-inventory.py
# Snapshots are per-environment (airbyte-snapshot-production.json,
# airbyte-snapshot-qa.json) and hold connector configuration, redacted but not
# worth committing.
airbyte-snapshot*.json
airbyte-findings.md

# Throwaway baseline copies of models, cut from origin/main with `git show` and
# built alongside the real model so `ol-dbt diff` can compare old against new.
**/models/**/*_pre.sql
