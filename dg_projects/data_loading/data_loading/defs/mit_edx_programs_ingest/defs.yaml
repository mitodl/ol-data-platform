---
type: dagster_dlt.DltLoadCollectionComponent

attributes:
  loads:
  - source: .loads.mit_edx_programs_load_source
    pipeline: .loads.mit_edx_programs_pipeline
    translation:
      group_name: ingestion
      key: "{{ resource.name }}"
      key_prefix: "ol_warehouse_raw_data"
      description: "Raw MIT-authored edX.org program metadata ingested from the edX\
        \ Programs API (JWT client credentials)."
