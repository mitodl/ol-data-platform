# dlt Configuration
# This file contains non-sensitive configuration that can be committed to git.
# Credentials are managed via AWS IAM (IRSA for K8s, ~/.aws/credentials for local dev).

[runtime]
log_level = "INFO"
dlthub_telemetry = true

# Limit extraction parallelism to 1 worker.
#
# dlt's default workers=5 launches concurrent threads that all share a single
# non-thread-safe injectable-context dict.  When threads race to enter/exit
# their own context managers the dict ends up in an inconsistent state and
# dlt raises ContainerInjectableContextMangled.  Using a single worker makes
# the context stack strictly sequential and eliminates the race.
[extract]
workers = 1
max_parallel_items = 20

# EdX.org S3 Source Configuration
# Path matches dlt's lookup: sources.<module_name>.<source_name>.<param>
[sources.loads.edxorg_s3_source]
bucket_url = "s3://ol-data-lake-landing-zone-production/edxorg-raw-data/edxorg/raw_data/"

# Local Development Destination (named destination)
# Writes Parquet files to local disk for fast iteration
[destination.local]
destination_type = "filesystem"
bucket_url = "file:///tmp/.dlt/data"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# Podcast RSS destinations (used by podcast_rss_ingest)
[destination.podcast_local]
destination_type = "filesystem"
bucket_url = "file:///tmp/.dlt/data/podcast"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.podcast_qa]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-qa/podcast"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.podcast_production]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-production/podcast"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# QA Destination - Filesystem with Iceberg + Glue Catalog (named destination)
# Writes Iceberg tables to S3 and registers in ol_warehouse_qa_raw Glue database
[destination.qa]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-qa/edxorg"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# Production Destination - Filesystem with Iceberg + Glue Catalog (named destination)
# Writes Iceberg tables to S3 and registers in ol_warehouse_production_raw Glue database
[destination.production]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-production/edxorg"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# Iceberg Catalog Configuration
#
# dlt reads [iceberg_catalog] (flat, not named sub-sections).
# The "aws_glue" catalog is defined in .dlt/.pyiceberg.yaml; pyiceberg's
# load_catalog mechanism handles Glue natively via boto3/IRSA.
# The target Glue database is determined by dataset_name at pipeline
# runtime (e.g. "ol_warehouse_production_raw"), not here.
[iceberg_catalog]
iceberg_catalog_name = "aws_glue"

# Note: Destination is auto-selected based on DAGSTER_ENVIRONMENT:
# - dev/ci -> local (filesystem, local disk, no Iceberg)
# - qa -> qa (filesystem + Iceberg, S3 + ol_warehouse_qa_raw Glue catalog)
# - production -> production (filesystem + Iceberg, S3 + ol_warehouse_production_raw Glue catalog)

# =============================================================================
# REST API / dlt sources
# Each source gets its own named destinations so S3 prefixes stay isolated.
# All share the same Iceberg catalog configuration.
# =============================================================================

# MIT Climate Portal
[destination.mit_climate_local]
destination_type = "filesystem"
bucket_url = "file:///tmp/.dlt/data/mit_climate"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.mit_climate_qa]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-qa/mit_climate"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.mit_climate_production]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-production/mit_climate"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# MIT Professional Education
[destination.mitpe_local]
destination_type = "filesystem"
bucket_url = "file:///tmp/.dlt/data/mitpe"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.mitpe_qa]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-qa/mitpe"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.mitpe_production]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-production/mitpe"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# Open Learning Library
[destination.oll_local]
destination_type = "filesystem"
bucket_url = "file:///tmp/.dlt/data/oll"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.oll_qa]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-qa/oll"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.oll_production]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-production/oll"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

# MIT edX Programs
[destination.mit_edx_programs_local]
destination_type = "filesystem"
bucket_url = "file:///tmp/.dlt/data/mit_edx_programs"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.mit_edx_programs_qa]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-qa/mit_edx_programs"
layout = "{table_name}/{load_id}.{file_id}.{ext}"

[destination.mit_edx_programs_production]
destination_type = "filesystem"
bucket_url = "s3://ol-data-lake-raw-production/mit_edx_programs"
layout = "{table_name}/{load_id}.{file_id}.{ext}"
